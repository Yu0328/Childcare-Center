# Real children's personal data and reference samples — never commit.
# Generic pattern rather than exact filenames, so this never has to name a real child.
*.docx
*.pdf

# App-exported data backups (full plaintext PII: names, birthdates, notes, photos).
c-form-backup-*.json

node_modules/
dist/
site/
.superpowers/
.claude/
